{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build && npm --prefix backend install"
  },
  "deploy": {
    "startCommand": "node backend/src/server.js"
  }
}